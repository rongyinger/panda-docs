{
  "name": "panda-docs",
  "version": "1.0.0",
  "description": "熊猫算力知识库站点",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev docs",
    "extract-home": "node scripts/extract-home.mjs",
    "build": "node scripts/extract-home.mjs && vitepress build docs",
    "build:opsbox": "node scripts/extract-home.mjs && vitepress build docs && node scripts/sync-dist-to-docs.mjs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "vitepress": "^1.6.3",
    "vue": "^3.5.13"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"],
    "approvedBuilds": ["esbuild"]
  }
}
